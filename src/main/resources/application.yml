server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/scouts33/booking-cloud-config-repo.git
          search-paths:
            - '{application}'
            - '{application}/*'
